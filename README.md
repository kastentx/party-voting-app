# Party Voting App

Vote on things until there is only one winner left!

#### To Install:
1. `clone` these files to your machine
2. `cd` into the new directory
3. `npm install` command will install dependencies
